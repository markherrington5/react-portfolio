import React from "react";
import "../styles/photoHeader.css";

const PhotoHeader = () => (
    <h1 id="photoTitle">
        <div class="animatePhotoTitle shortDelay">Photography</div>
    </h1>
);

export default PhotoHeader;

